import React from 'react'

const Coordinates = (props) => {
  return (
    <div>  const coordinates = (
        <div className="coordinates">
          <p>{`Move: ${props.moveCounter}`}</p>
          <p>{`X: ${props.position.x}`}</p>
          <p>{`Y: ${props.position.y}`}</p>
        </div>
      );</div>
  )
}

export default Coordinates